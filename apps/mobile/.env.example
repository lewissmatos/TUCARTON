# Public development endpoint reachable from the physical device.
# Use your Mac's LAN IP while both devices are on the same Wi-Fi network.
# Use your Mac's Bonjour hostname (scutil --get LocalHostName), not a changing Wi-Fi IP.
EXPO_PUBLIC_API_URL=http://YOUR-MAC-HOSTNAME.local:3000
