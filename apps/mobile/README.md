# @tucarton/mobile

Expo / React Native mobile shell for Android and iOS. It contains routing and package configuration only; local SQLite repositories, secure credential handling, QR scans, and sync behavior are intentionally deferred to approved feature work items.

Run `npm run mobile:start` from the repository root.
