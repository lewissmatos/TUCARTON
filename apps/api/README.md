# @tucarton/api

NestJS modular-monolith API. It exposes a versioned health endpoint, generated OpenAPI document, local account registration/login with phone and passcode, and the shared-ledger API. Ledger synchronization remains deferred.

Run `npm run api:dev` from the repository root, then open `/api/v1/health` or `/api/docs`.
